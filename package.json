{
  "name": "euro-city-stays",
  "version": "1.0.0",
  "private": true,
  "description": "Euro City Stays static site with Jest tests",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "jsdom"
  }
}
